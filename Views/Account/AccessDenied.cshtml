@{
    ViewData["Title"] = "Akses Ditolak";
}

<div class="container mt-5 text-center">
    <h2 class="text-danger">Akses Ditolak</h2>
    <p>Anda tidak memiliki izin untuk membuka halaman ini.</p>
    <a asp-controller="Account" asp-action="Login" class="btn btn-primary">Kembali ke Login</a>
</div>
